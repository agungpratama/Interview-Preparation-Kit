    // for(int i = 0; i < sizeof(queries) / sizeof(queries[0]); i++) {
    //     //do query
    //     int startIndex = queries[i][0] - 1;
    //     int endIndex = queries[i][1] - 1;
    //     int valueToAdd = queries[i][2];

    //     cout << "startIndex: " << startIndex << " endIndex:" << endIndex << " valueToAdd: " << valueToAdd << endIndex;

    //     for(int j = startIndex; j <= endIndex; j++) {
    //         mapSum[j] = mapSum[j] + valueToAdd;
    //         biggestNumber = max(biggestNumber,mapSum[j]);
    //     }
    // }
